# قائمة مهام التطبيق - Smart Dental Platform
## تاريخ الإنشاء: 2025-10-30

---

## 🔴 مشاكل حرجة تحتاج إصلاح فوري

### 1. نظام المصادقة والصلاحيات
- [ ] **CRITICAL**: تحديث المستخدم الافتراضي ليكون مدير منصة (✅ تم)
- [ ] **HIGH**: إضافة صلاحيات مدير العيادة في sharedClinicData
- [ ] **HIGH**: ربط صلاحيات الموظفين بجدول قاعدة البيانات
- [ ] **MEDIUM**: إضافة واجهة لتعيين الأدوار للموظفين

### 2. شريط تبديل الأدوار (ClinicRoleSwitcherBar)
- [x] **CRITICAL**: إصلاح منطق canSwitchClinic (✅ تم)
- [ ] **HIGH**: حفظ العيادة المختارة في ClinicContext
- [ ] **HIGH**: تحديث جميع الصفحات لتستخدم selectedClinicId من Context
- [ ] **MEDIUM**: إضافة رسالة تأكيد عند تبديل العيادة
- [ ] **LOW**: إضافة animation عند تبديل الأدوار

### 3. البيانات الوهمية المطلوب إزالتها
- [ ] **HIGH**: إزالة mockUsers من DentistHub.tsx
- [ ] **HIGH**: إزالة البيانات الثابتة من sharedClinicData.ts
- [ ] **HIGH**: إنشاء migration لقاعدة البيانات للبيانات الأساسية
- [ ] **MEDIUM**: إضافة seed data للتجربة فقط

---

## 🟡 ميزات مطلوب إكمالها

### 4. إدارة المرضى
- [ ] **HIGH**: ربط AddPatientDialog بقاعدة البيانات الفعلية
- [ ] **HIGH**: إضافة validation للبيانات المدخلة
- [ ] **HIGH**: عرض المرضى من قاعدة البيانات حسب العيادة
- [ ] **MEDIUM**: إضافة بحث وفلترة للمرضى
- [ ] **MEDIUM**: إضافة صفحة تفاصيل المريض
- [ ] **LOW**: إضافة تصدير قائمة المرضى

### 5. إدارة المواعيد
- [ ] **HIGH**: ربط AddReservationDialog بقاعدة البيانات
- [ ] **HIGH**: عرض المواعيد من قاعدة البيانات حسب العيادة
- [ ] **HIGH**: إضافة تحديثات فورية للمواعيد (Realtime)
- [ ] **MEDIUM**: إضافة تنبيهات للمواعيد القادمة
- [ ] **MEDIUM**: إضافة calendar view للمواعيد
- [ ] **LOW**: إضافة خاصية السحب والإفلات لتغيير المواعيد

### 6. نظام الإشعارات
- [ ] **HIGH**: إنشاء جدول notifications في قاعدة البيانات
- [ ] **HIGH**: ربط الإشعارات بالأحداث (مواعيد جديدة، مرضى جدد)
- [ ] **HIGH**: عرض الإشعارات في UnifiedNotifications
- [ ] **MEDIUM**: إضافة تنبيهات push للإشعارات المهمة
- [ ] **MEDIUM**: إضافة فلترة للإشعارات حسب النوع
- [ ] **LOW**: إضافة إعدادات تفضيلات الإشعارات

### 7. المهام والتذكيرات
- [ ] **HIGH**: ربط StaffTasks بقاعدة البيانات
- [ ] **HIGH**: ربط StaffReminders بقاعدة البيانات
- [ ] **HIGH**: إضافة واجهة لإنشاء مهام جديدة
- [ ] **MEDIUM**: إضافة نظام الموافقة على المهام
- [ ] **MEDIUM**: إضافة تذكيرات تلقائية
- [ ] **LOW**: إضافة تقارير إنجاز المهام

---

## 🟢 تحسينات واختبارات

### 8. العيادة الذكية (Smart Clinic)
- [ ] **HIGH**: التأكد من ربط جميع البيانات بالعيادة المختارة
- [ ] **HIGH**: اختبار AI Assistant مع البيانات الفعلية
- [ ] **MEDIUM**: إضافة تقارير ذكية بناءً على البيانات
- [ ] **MEDIUM**: تحسين واجهة المستخدم للعيادة الذكية
- [ ] **LOW**: إضافة رسوم بيانية تفاعلية

### 9. إدارة العيادات
- [ ] **HIGH**: ربط ClinicsManager بقاعدة البيانات
- [ ] **HIGH**: إضافة إمكانية إنشاء عيادة جديدة
- [ ] **HIGH**: إضافة إعدادات العيادة (أوقات العمل، الخدمات)
- [ ] **MEDIUM**: إضافة إدارة الطاقم لكل عيادة
- [ ] **MEDIUM**: إضافة إدارة الصلاحيات لكل عيادة
- [ ] **LOW**: إضافة تقارير لكل عيادة

### 10. المالية والتقارير
- [ ] **HIGH**: إنشاء جدول financial_records في قاعدة البيانات
- [ ] **HIGH**: ربط المدفوعات بالمواعيد والعلاجات
- [ ] **MEDIUM**: إضافة تقارير مالية شهرية
- [ ] **MEDIUM**: إضافة dashboard للمالية
- [ ] **LOW**: إضافة تصدير التقارير المالية

### 11. المخزون
- [ ] **HIGH**: ربط الم��زون بقاعدة البيانات
- [ ] **HIGH**: إضافة تنبيهات للمواد قليلة المخزون
- [ ] **MEDIUM**: إضافة نظام طلبات الشراء
- [ ] **MEDIUM**: ربط المخزون بالعلاجات
- [ ] **LOW**: إضافة تقارير حركة المخزون

---

## 🔵 اختبارات فعلية مطلوبة

### 12. سيناريوهات الاختبار
- [ ] **تسجيل الدخول كمدير منصة**
  - [ ] التحقق من ظهور جميع العيادات
  - [ ] التحقق من إمكانية التبديل بين العيادات
  - [ ] التحقق من إمكانية التبديل بين الأدوار

- [ ] **تسجيل الدخول كطبيب (مالك عيادة)**
  - [ ] التحقق من ظهور عياداته فقط
  - [ ] التحقق من إمكانية التبديل بين عياداته
  - [ ] التحقق من إمكانية التبديل بين الأدوار في عيادته

- [ ] **تسجيل الدخول كموظف**
  - [ ] التحقق من عدم إمكانية تبديل العيادة
  - [ ] التحقق من عرض اسم العيادة فقط
  - [ ] التحقق من الصلاحيات المحدودة

- [ ] **إضافة مريض جديد**
  - [ ] من مركز الأطباء
  - [ ] من إدارة العيادة
  - [ ] التحقق من ظهوره في قائمة المرضى
  - [ ] التحقق من إشعار المريض الجديد

- [ ] **إضافة موعد جديد**
  - [ ] من مركز الأطباء
  - [ ] من إدارة العيادة
  - [ ] التحقق من ظهوره في التقويم
  - [ ] التحقق من إشعار الموعد الجديد
  - [ ] التحقق من التذكير قبل الموعد

- [ ] **نظام الإشعارات**
  - [ ] إشعار مريض جديد
  - [ ] إشعار موعد جديد
  - [ ] إشعار تذكير موعد
  - [ ] إشعار مهمة جديدة
  - [ ] إشعار مخزون منخفض

---

## 📊 قاعدة البيانات المطلوبة

### 13. جداول مطلوب إنشاؤها
```sql
-- Clinics Table
CREATE TABLE clinics (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  owner_id UUID REFERENCES auth.users(id),
  name TEXT NOT NULL,
  name_ar TEXT NOT NULL,
  address TEXT,
  city TEXT,
  phone TEXT,
  email TEXT,
  online_booking_enabled BOOLEAN DEFAULT false,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

-- Clinic Staff Table
CREATE TABLE clinic_staff (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  clinic_id UUID REFERENCES clinics(id),
  user_id UUID REFERENCES auth.users(id),
  role TEXT NOT NULL, -- doctor, nurse, assistant, receptionist, manager
  permissions JSONB,
  created_at TIMESTAMP DEFAULT NOW()
);

-- Patients Table
CREATE TABLE patients (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  clinic_id UUID REFERENCES clinics(id),
  name TEXT NOT NULL,
  phone TEXT NOT NULL,
  email TEXT,
  date_of_birth DATE,
  gender TEXT,
  address TEXT,
  medical_history JSONB,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

-- Appointments Table
CREATE TABLE appointments (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  clinic_id UUID REFERENCES clinics(id),
  patient_id UUID REFERENCES patients(id),
  doctor_id UUID REFERENCES auth.users(id),
  date DATE NOT NULL,
  time TIME NOT NULL,
  duration INTEGER DEFAULT 30,
  treatment TEXT,
  status TEXT DEFAULT 'scheduled',
  notes TEXT,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

-- Notifications Table
CREATE TABLE notifications (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES auth.users(id),
  clinic_id UUID REFERENCES clinics(id),
  type TEXT NOT NULL,
  title TEXT NOT NULL,
  message TEXT,
  related_id UUID,
  related_type TEXT,
  is_read BOOLEAN DEFAULT false,
  created_at TIMESTAMP DEFAULT NOW()
);

-- Staff Tasks Table
CREATE TABLE staff_tasks (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  clinic_id UUID REFERENCES clinics(id),
  from_staff_id UUID REFERENCES auth.users(id),
  to_staff_id UUID REFERENCES auth.users(id),
  title TEXT NOT NULL,
  description TEXT,
  priority TEXT DEFAULT 'medium',
  status TEXT DEFAULT 'pending',
  due_date TIMESTAMP,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

-- Staff Reminders Table
CREATE TABLE staff_reminders (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  clinic_id UUID REFERENCES clinics(id),
  staff_id UUID REFERENCES auth.users(id),
  title TEXT NOT NULL,
  reminder_time TIMESTAMP NOT NULL,
  reminder_type TEXT,
  related_id UUID,
  status TEXT DEFAULT 'pending',
  created_at TIMESTAMP DEFAULT NOW()
);
```

### 14. RLS Policies مطلوبة
- [ ] **Clinics**: المالك والطاقم فقط
- [ ] **Patients**: بناءً على clinic_id والصلاحيات
- [ ] **Appointments**: بناءً على clinic_id والصلاحيات
- [ ] **Notifications**: المستخدم الخاص به فقط
- [ ] **Staff Tasks**: المرسل والمستقبل فقط
- [ ] **Staff Reminders**: الموظف الخاص به فقط

---

## 🎯 أولويات التنفيذ (بالترتيب)

### المرحلة 1: الأساسيات (أسبوع 1)
1. إصلاح نظام المصادقة والصلاحيات ✅
2. إنشاء جداول قاعدة البيانات الأساسية
3. إضافة RLS policies
4. ربط ClinicContext بجميع الصفحات

### المرحلة 2: الوظائف الأساسية (أسبوع 2)
5. ربط إدارة المرضى بقاعدة البيانات
6. ربط إدارة المواعيد بقاعدة البيانات
7. إضافة نظام الإشعارات الأساسي
8. اختبار السيناريوهات الأساسية

### المرحلة 3: الميزات المتقدمة (أسبوع 3)
9. إضافة المهام والتذكيرات
10. إضافة المالية والتقارير
11. تحسين العيادة الذكية
12. إضافة إدارة المخزون

### المرحلة 4: التحسينات والاختبار (أسبوع 4)
13. اختبار جميع السيناريوهات
14. تحسين الأداء
15. إضافة التوثيق
16. إصلاح الأخطاء

---

## 📝 ملاحظات مهمة

### البيانات الوهمية الحالية الموجودة في:
1. **client/pages/DentistHub.tsx**:
   - mockUsers (lines 32-71) - يجب إزالتها
   
2. **client/services/sharedClinicData.ts**:
   - patients (hardcoded data)
   - appointments (hardcoded data)
   - treatments (hardcoded data)
   - staff (hardcoded data)
   - clinics (hardcoded data)
   - يجب تحويلها لاستعلامات قاعدة البيانات

3. **client/contexts/AuthContext.tsx**:
   - defaultUser - تم تحديثه ليكون PLATFORM_ADMIN ✅

### الملفات المطلوب تعديلها بشكل كامل:
1. `client/services/sharedClinicData.ts` - تحويل إلى service حقيقي
2. `client/pages/DentistHub.tsx` - إزالة البيانات الوهمية
3. `client/contexts/ClinicContext.tsx` - إضافة المزيد من الوظائف
4. `client/components/ClinicRoleSwitcherBar.tsx` - ✅ تم التحديث

### الملفات المطلوب إنشاؤها:
1. `client/services/clinicService.ts` - خدمة العيادات الفعلية
2. `client/services/patientService.ts` - خدمة المرضى
3. `client/services/appointmentService.ts` - خدمة المواعيد
4. `client/services/notificationService.ts` - خدمة الإشعارات
5. `client/hooks/useClinicData.ts` - hook للبيانات
6. `client/hooks/useNotifications.ts` - hook للإشعارات

---

## 🚀 التحديثات المطلوبة فوراً

### إصلاحات عاجلة:
1. ✅ تغيير المستخدم الافتراضي إلى PLATFORM_ADMIN
2. ✅ إصلاح منطق canSwitchClinic
3. ⏳ إنشاء migration لقاعدة البيانات
4. ⏳ ربط البيانات الحقيقية بمركز الأطباء

---

## 📞 للمتابعة
- هل تريد البدء بإنشاء migrations لقاعدة البيانات؟
- هل تريد إنشاء services للعمليات الفعلية؟
- هل تريد اختبار السيناريوهات المحددة؟
